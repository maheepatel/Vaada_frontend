type Props = { publiclyNamed: boolean; name: string; onPubliclyNamedChange: (value: boolean) => void; onNameChange: (value: string) => void };

export function AttributionChoice({ publiclyNamed, name, onPubliclyNamedChange, onNameChange }: Props) {
  return <fieldset className="attribution-choice"><legend>Public credit for this contribution</legend><p>Your saved account preference is selected by default. You can change it here. Your email is never published.</p><div className="submission-choice-grid"><label className={!publiclyNamed ? "selected" : ""}><input name="credit" type="radio" checked={!publiclyNamed} onChange={() => onPubliclyNamedChange(false)} /><span><strong>Keep my name private</strong><small>Your account remains attached only to the private audit trail.</small></span></label><label className={publiclyNamed ? "selected" : ""}><input name="credit" type="radio" checked={publiclyNamed} onChange={() => onPubliclyNamedChange(true)} /><span><strong>Credit my display name</strong><small>Your name appears only if a reviewer accepts this contribution.</small></span></label></div>{publiclyNamed && <label>Public display name<input required minLength={2} maxLength={120} value={name} onChange={(event) => onNameChange(event.target.value)} /></label>}</fieldset>;
}
