import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";

const steps = [["01","Collect","A citizen shares a public source or uploads a document. Every submission remains private while queued."],["02","Extract","AI proposes fields and flags uncertainty. It cannot publish, verify evidence or assign accountability."],["03","Verify","A human checks the original source, scope, authority, dates, duplicates and whether the wording is fair."],["04","Publish","An accepted promise receives a permanent public record. A receipt remains attached to it."],["05","Update","New evidence can support or challenge progress. Only accepted proof changes public status."],["06","Correct","Every material correction creates a visible history event. Published records are never silently rewritten."]];
const questions = [["What qualifies as a promise?","A specific, attributable public commitment with a source, an identifiable place or group, and an outcome the public could reasonably verify."],["Can AI publish or mark a promise completed?","No. AI only extracts a draft from links, images and documents. A human reviewer must inspect the original proof and make the decision."],["Who can submit progress or completion proof?","Anyone can submit public evidence. Government authority submissions use the same evidence trail and never bypass review."],["How is progress changed?","A reviewer accepts evidence, records a public note, chooses the verified percentage and, when justified, marks the promise completed."],["Can a published record be corrected?","Yes. Corrections create a visible history event; the earlier record is never silently erased."]];

export default function MethodologyPage(){return <main className="site-shell route-shell"><SiteHeader/><section className="route-hero methodology-hero"><p className="eyebrow">HOW VAADA WORKS</p><h1>AI assists.<br/><span>Humans decide.</span></h1><p>Vaada separates collection, verification and publication so speed never quietly becomes certainty.</p></section><section className="method-page"><p className="eyebrow">THE PUBLIC WORKFLOW</p><div className="method-accordions">{steps.map(([number,title,copy],index)=><details key={number} open={index===0}><summary><span>{number}</span><strong>{title}</strong><i>＋</i></summary><p>{copy}</p></details>)}</div><section id="editorial" className="policy-block faq-block"><p className="eyebrow">COMMON QUESTIONS</p><h2>Trust should be inspectable.</h2><div className="faq-accordions">{questions.map(([question,answer],index)=><details key={question} open={index===0}><summary><strong>{question}</strong><i>＋</i></summary><p>{answer}</p></details>)}</div></section><section id="corrections" className="policy-block"><p className="eyebrow">CORRECTIONS</p><h2>A record can change. Its history cannot disappear.</h2><p>People can challenge any field or evidence item. Reviewers record the reason, decision and resulting changes in an append only audit trail.</p></section></section><SiteFooter/></main>}
